{% extends 'base.html.twig' %}

{% block title %}Créer une catégorie{% endblock %}

{% block body %}
<h1>Créer une catégorie</h1>

{{form_start(formView)}}
{{form_widget(formView)}}

<button type="submit" class="btn btn-primary">
    <i class="class fas fa-save"></i>
    Créer le produit
</button>

{{form_end(formView)}}
{% endblock %}
